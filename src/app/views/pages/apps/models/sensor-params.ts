export interface SensorParams {
    device_sn: string;
    from: string;
    until: string;
    reading_type: string
    // start_mrid: number;
    // end_mrid: number;
    // output_format: string;
    // page_num: number;
    // per_page: number;
    // device_depth: boolean;
    // sort_by: string;
}